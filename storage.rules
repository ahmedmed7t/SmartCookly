rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/scan_image.jpg {
      allow read: if true;  // Public read for OpenAI
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
